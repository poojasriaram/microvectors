
/**
 * Legacy hook for analytics. 
 * Most tracking is now handled automatically by the central AnalyticsService 
 * initialized in main.tsx.
 */
export const useAnalytics = () => {
    // Current auto-tracking is handled in src/lib/analytics.ts
    return null;
};
